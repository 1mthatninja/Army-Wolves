const TICK_RATE = 50;

const DELTA_TIME =
  1 / TICK_RATE;

const STEP =
  DELTA_TIME;

const PLAYER_SPEED = 160;

const PLAYER_RADIUS = 15;

module.exports = {
  TICK_RATE,
  DELTA_TIME,
  STEP,
  PLAYER_SPEED,
  PLAYER_RADIUS
};